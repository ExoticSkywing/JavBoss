# JAV 作品入库生命周期

本文档定义 JavBoss 自上而下入库通路的权威领域语义。实现、接口和界面若与本文冲突，
应以本文中的不变量为准并补充迁移，而不是继续维护第二套“待处理作品”。

## 一条核心不变量

`jav` 是全局唯一作品全集。任何时间、任何来源、任何次数输入的同一语义番号都只能指向
同一个 `jav.id`。

番号的展示形式保存在 `jav.code`，用于唯一性和查找的键是 `jav.normalized_code`。大小写、
连字符和下划线等表现差异不能创建第二部作品。数据库唯一约束是最终并发防线，前端去重、
批次去重和进程锁都不能代替它。

真实文件也不是另一种作品。作品是否已入库由有效文件位置实时派生：

- `pending`：没有有效的 `video_location`；
- `imported`：至少存在一个有效的 `video_location`。

不得保存一个可与真实文件关系漂移的 `is_imported` 布尔值。

## 番号输入的产品语义

输入器容忍混杂文字、空白、逗号和一行多个番号。系统负责解析、标准化以及批内和全局去重，
用户不需要预先整理数据。

一次提交只产生三种业务结果：

1. 无新增：识别出的唯一番号全部已经存在；
2. 部分新增：一部分已经存在，另一部分首次加入作品库；
3. 全部新增：识别出的唯一番号全部首次加入作品库。

其中“已有”同时包含未入库和已入库作品。批内重复、以前输入过、已经扫盘入库都只是解释来源，
不是互斥的作品状态。纯文字备注静默忽略；包含数字但疑似漏识别的片段作为可展开警告；一个番号
都没有识别出来时返回输入错误，不能显示“无新增”。

`jav_input_batch` 和 `jav_input_item` 可以保留原文与来源，作用仅是审计收据。删除输入历史或清理
工作区永远不能删除 `jav`、改变库存状态或让番号重新成为增量。

## 同一作品的状态机

库存状态和工作流阶段是两个正交维度：

```text
脏数据输入
  -> 原子命中或创建 Jav
  -> metadata_pending
  -> magnet_collecting
  -> magnet_review
  -> ready_to_download
  -> download_submitted
  -> 扫盘关联真实文件
  -> imported
```

第一阶段可以只持久化当前已经实现的最小阶段；后续阶段只能在同一个 `jav_id` 下扩展，不能再建立
一套候选作品实体。115 下载提交由未来服务负责，本项目当前只预留边界。

当前实际持久化 `metadata_pending / magnet_collecting / imported`。若最后一个有效文件位置被隐藏、
删除、替换或解除关联，库存立即回到 `pending`；已有元数据的作品回到 `magnet_collecting`，裸番号
回到 `metadata_pending`。仍有同一文件镜像位置时保持 `imported`，目录暂时离线不视为作品出库。

## 提前刮削与扫盘转正

新建的未入库 `Jav` 应立即进入后台元数据刮削队列。标题、封面、女优、片商、系列和标签都直接
写回这一条作品记录。扫盘仍必须完成目录发现、文件指纹和位置关联，但当它识别出番号时应先按
`normalized_code` 查找已有作品：

```text
未入库 Jav（已有元数据）
  + 扫盘发现的真实文件
  = 同一个 Jav 自动派生为已入库
```

这会复用已抓取元数据并避免再次联网刮削，不等于跳过扫盘。同一路径被不同指纹的新文件替换时，
必须清除旧 `jav_id` 并重新识别，不能让新文件继承旧作品身份。扫描器同时保存一个不参与展示的
文件系统身份（设备/inode 或平台等价字段）；旧记录首次升级时会主动复核一次，之后仍走大小、修改时间
和身份三项快速判断。

### 元数据缺项的处理

片商、系列和女优都是 `jav` 的关联元数据，不是“已入库文件”的属性。某个字段暂时没有抓到时，
保留作品和已抓到的其它字段，继续把它作为 `pending` 作品展示，并由对应的后台补全扫描再次尝试；
未知值不能伪造成“没有这部作品”。

这些字段按独立通路补全，互不阻塞：

- 标题和女优关系由主元数据扫描补全；已有标题但没有女优时仍继续扫描，不能因为标题已经存在就跳过；
- 片商由片商扫描补全，系列由本地系列扫描补全；没有英文系列提示的作品直接走 JavMenu，不必等待 Avmoo
  的全局 fallback；
- provider 返回的非空字段只填空位，已有值不被空响应擦除；返回番号与请求番号不一致的响应丢弃；
- Avsox 可以作为标题/女优的兜底来源，但它的“无码”判断不能写入通用元数据，无码状态由专门扫描确认；
- 资料源暂时找不到某字段时保持“待补全”，不创建“未知女优/未知片商/未知系列”实体；人工编辑可以直接
  赋值，后续再提供强制重试或明确的“来源确认无此字段”状态。

档案补全也遵循同一原则。只有缺中文名且其它档案字段已齐全的女优会进入较长的重试窗口，避免每分钟
重复请求相同来源；这不是把缺项当作已完成，而是降低无意义请求，服务重启后仍可再次尝试。

女优索引按所有可见关联作品聚合。只有多人作品的女优也必须出现；封面选择优先可见单体作品，
没有单体作品时回退到可见多人作品。这样“没有单体封面”不会再被误判为“没有女优”。详情、收藏、
封面选择和档案补全使用同一套可见性规则。

实体页为空只表示当前没有任何作品关联到该实体，不能表示作品元数据刮削失败。作品页的库存切换
仍是判断真实文件状态的唯一入口；实体页展示的是作品全集的元数据视图。

## 陈列馆的信息架构

陈列馆继续使用同一套作品列表、卡片和详情，只提供显眼的库存切换：`全部 / 未入库 / 已入库`。
未入库不是新的侧栏实体或页面。

番号索引也遵循同一语义：它基于完整的 `jav` 作品集合聚合，逐项显示总数、未入库数和已入库数，
并提供独立的库存筛选。点击番号后应把当前库存条件带入作品列表，不能因为入口是番号而退回只看真实文件的旧逻辑。

未入库卡片显示库存徽标和当前工作流阶段。依赖真实文件的播放、截图和视频管理能力保持不可用。
磁链候选阶段实现后，这里再出现唯一下一步操作“筛选磁链”；第一阶段不放置无法完成的空按钮。
扫盘关联文件后，卡片自然恢复原生能力，元数据、收藏、标签和详情不做搬运。

## 磁链候选与质量事实

磁链候选属于作品，至少以 `(jav_id, info_hash)` 幂等唯一。每部作品最多选中一个最佳候选。
候选需要分别保存自动事实和人工事实，未知值不能等同于否：

- 自动事实：名称、大小、文件数、来源 HD/中文字幕标记、发布时间、抓取时间；
- 人工事实：肉眼清晰度、是否确认 1080P、片头广告、水印、跑马灯、有码/无码、采用或淘汰原因、备注。

当前质量目标以约 5–10 GiB、肉眼高清 1080P 为首要条件；无片头广告、无水印/跑马灯为次要条件。
有码/无码的优先级描述目前存在冲突，因此第一阶段只记录客观状态，不硬编码排序。积累足够人工
标注后，自动评分必须基于这些可解释事实训练和校准，人工仍可覆盖最终选择。

## 文件唯一性与冲突

目标业务是每个番号在 115 中只有一份高质量真实文件，但现有系统允许同一视频有多个位置。
在数据得到充分审计之前，不直接给 `video_location.jav_id` 添加简单唯一索引：

- 同一文件指纹的多个位置可作为同一资产的镜像处理；
- 不同指纹不能静默关联到同一番号，应报告媒体冲突并等待处理；
- 后续可采用“选定资产”关系或活动位置的条件唯一约束来落实物理唯一性。

## 第一阶段验收标准

- 多种表现形式的同一番号在数据库中只有一个 `Jav`；
- 任意重复提交都只增加此前不存在的作品；
- 历史 accepted 数据迁移为正式 `Jav`，删除历史不会释放番号；
- 未入库作品能被后台提前刮削并出现在原生陈列馆；
- `/jav` 能按 `all / pending / imported` 查询且响应明确返回库存状态；
- 扫盘发现文件后关联原 `Jav`，库存状态自动转为已入库；
- 同路径替换成新文件时不继承旧 `jav_id`；
- 数据库迁移后的实际 schema 与 GORM 模型完全一致。
